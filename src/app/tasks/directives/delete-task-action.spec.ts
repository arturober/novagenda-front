import { DeleteTaskAction } from './delete-task-action';

describe('DeleteTaskAction', () => {
  it('should create an instance', () => {
    const directive = new DeleteTaskAction();
    expect(directive).toBeTruthy();
  });
});
