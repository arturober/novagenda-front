import { GoogleLoginBtn } from './google-login-btn';

describe('GoogleLoginBtn', () => {
  it('should create an instance', () => {
    const directive = new GoogleLoginBtn();
    expect(directive).toBeTruthy();
  });
});
